are project numbers always integers? what's the range ?
figure out how to extract the data


TEREN - max number of chars ? All edittext ? dropdowns ? Constraints ?

Check Databases Design for pt2. - > each activity = new table in db ? Or each element = column ?

On pause ? Background ?


GET GPS LOCATION

